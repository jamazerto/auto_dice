{
  "manifest_version": 2,

  "name": "Getting started example",
  "description": "This extension allows the user to roll dice whenever a a webpage says (xdx + x)",
  "version": "1.0",

  "browser_action": {
    "default_icon": "d20.png"
  },
  	  
  "background": {
    "scripts": ["action.js"]
  },
  "permissions": [
    "activeTab"
  ]
}